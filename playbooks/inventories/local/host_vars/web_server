---

user: "{{ ansible_user }}"
group: vagrant
domain: example.com
host_port: 80
admin_email: admin@example.com
project_name: simpleproject
app_dir: "{{ ansible_env.HOME }}/{{ project_name }}"
venv_path: "{{ app_dir }}/venv"
enable_ssl: false
